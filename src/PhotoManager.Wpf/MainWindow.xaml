<Window x:Class="PhotoManager.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding L.Title}" Width="1280" Height="800" MinWidth="900" MinHeight="600"
        FlowDirection="{Binding L.Flow}" WindowStartupLocation="CenterScreen">
  <DockPanel>
    <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}" Padding="8">
      <StackPanel Orientation="Horizontal">
        <Button Content="{Binding L.AddFolder}" Command="{Binding ScanCommand}"/>
        <Button Content="{Binding L.Cancel}" Command="{Binding CancelCommand}"/>
        <Button Content="{Binding L.CheckMissing}" Command="{Binding CheckMissingCommand}"/>
        <Button Content="{Binding L.Backup}" Command="{Binding BackupCommand}"/>
        <Button Content="{Binding L.Language}" Command="{Binding ToggleLanguageCommand}"/>
      </StackPanel>
    </Border>

    <Border DockPanel.Dock="Top" Padding="10,8">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="3*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding L.Search}" Margin="3"/>
        <TextBox Grid.Column="1" Text="{Binding TagFilter, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding L.TagFilter}" Margin="3"/>
        <ComboBox Grid.Column="2" ItemsSource="{Binding Ratings}" SelectedItem="{Binding MinimumRating}" ToolTip="{Binding L.Rating}" MinWidth="90" Margin="3"/>
        <TextBlock Grid.Column="3" Text="{Binding TotalCount, StringFormat={}{0:N0}}" VerticalAlignment="Center" Margin="12,0"/>
      </Grid>
    </Border>

    <Border DockPanel.Dock="Bottom" Background="{StaticResource SurfaceBrush}" Padding="9">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Status}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
        <ProgressBar Grid.Column="1" Height="7" Maximum="100" Value="{Binding ProgressValue}" VerticalAlignment="Center"/>
      </Grid>
    </Border>

    <Grid>
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="330"/></Grid.ColumnDefinitions>
      <DockPanel Grid.Column="0">
        <Button DockPanel.Dock="Bottom" Content="{Binding L.LoadMore}" Command="{Binding LoadMoreCommand}" HorizontalAlignment="Center" Margin="8"/>
        <ListView x:Name="PhotoList" ItemsSource="{Binding Photos}" SelectionMode="Extended" Background="Transparent" BorderThickness="0"
                  ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  SelectionChanged="PhotoList_OnSelectionChanged">
          <ListView.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel/></ItemsPanelTemplate></ListView.ItemsPanel>
          <ListView.ItemTemplate>
            <DataTemplate>
              <Grid Height="116" Margin="4">
                <Grid.ColumnDefinitions><ColumnDefinition Width="132"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Width="116" Height="104" Background="#101216" CornerRadius="6">
                  <Image Source="{Binding Thumbnail}" Stretch="Uniform"/>
                </Border>
                <StackPanel Grid.Column="1" Margin="10,8">
                  <TextBlock Text="{Binding FileName}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                  <TextBlock Text="{Binding Details}" Foreground="{StaticResource MutedBrush}" Margin="0,7,0,0"/>
                  <TextBlock Text="{Binding Stars}" Foreground="#FFCB45" FontSize="16"/>
                  <TextBlock Text="{Binding Path}" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </DockPanel>

      <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" Padding="14" BorderBrush="#343A46" BorderThickness="1,0,0,0">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel>
            <Border Height="230" Background="#101216" CornerRadius="8" Margin="0,0,0,12">
              <Image Source="{Binding SelectedPhoto.Thumbnail}" Stretch="Uniform"/>
            </Border>
            <TextBlock Text="{Binding SelectedPhoto.FileName}" FontSize="17" FontWeight="SemiBold" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SelectedPhoto.Path}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,5,0,14"/>
            <TextBlock Text="{Binding L.TagInput}" Margin="0,4"/>
            <TextBox Text="{Binding TagInput, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
              <Button Content="{Binding L.AddTags}" Command="{Binding AddTagsCommand}"/>
              <Button Content="{Binding L.RemoveTag}" Command="{Binding RemoveTagCommand}"/>
            </StackPanel>
            <TextBlock Text="{Binding L.Rating}" Margin="0,9,0,4"/>
            <ComboBox ItemsSource="{Binding Ratings}" SelectedItem="{Binding SelectedRating}"/>
            <TextBlock Text="{Binding L.Notes}" Margin="0,12,0,4"/>
            <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="110" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            <Button Content="{Binding L.SaveNotes}" Command="{Binding SaveNotesCommand}" Margin="0,7"/>
          </StackPanel>
        </ScrollViewer>
      </Border>
    </Grid>
  </DockPanel>
</Window>
